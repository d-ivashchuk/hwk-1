# Scotto9 wired-only configuration

# Disable Bluetooth for wired USB-only operation
CONFIG_BT=n
CONFIG_ZMK_BLE=n

# USB settings
CONFIG_USB_DEVICE_PRODUCT="Scotto9"
CONFIG_USB_DEVICE_MANUFACTURER="ScottoKeebs"

# Disable power management features for wired use
CONFIG_ZMK_SLEEP=n
